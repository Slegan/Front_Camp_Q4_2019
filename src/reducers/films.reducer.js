
import ACTIONS from '../actions/actionsTypes';

const initialState = {
  name: '',
};

const recognition = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default recognition;
